<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mibibliotecaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiBibliotecaApp">
        <activity
            android:name=".presentationdomain.PorLeer"
            android:exported="true" />
        <activity
            android:name=".presentationdomain.leidos"
            android:exported="true" />
        <activity
            android:name=".presentationdomain.favoritos"
            android:exported="true" />
        <activity
            android:name=".presentationdomain.MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>